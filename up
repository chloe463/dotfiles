#!/bin/zsh

set -eux
set -o pipefail

# emulate GNU readlink's "readlink -f" behavior on Mac
# http://stackoverflow.com/questions/1055671/how-can-i-get-the-behavior-of-gnus-readlink-f-on-a-mac
TARGET_FILE=$0

cd `dirname $TARGET_FILE`
TARGET_FILE=`basename $TARGET_FILE`

# Iterate down a (possible) chain of symlinks
while [ -L "$TARGET_FILE" ]
do
    TARGET_FILE=`readlink $TARGET_FILE`
    cd `dirname $TARGET_FILE`
    TARGET_FILE=`basename $TARGET_FILE`
done

# Compute the canonicalized name by finding the physical path 
# for the directory we're in and appending the target file.
BASE_DIR=`pwd -P`
# PHYS_DIR=`pwd -P`
# RESULT=$PHYS_DIR/$TARGET_FILE
# echo $RESULT
# echo $(dirname $RESULT)
# BASE_DIR=$(dirname $RESULT)

function install_sheldon_plugins()
{
    if type sheldon > /dev/null 2>&1; then
        echo 'Installing sheldon plugins...'
        sheldon lock
    else
        echo 'ERROR: sheldon is not installed. Run: ./up brew' >&2
        return 1
    fi
}

function run_stow()
{
    if type stow > /dev/null 2>&1; then
        echo "Making symlinks of dot files..."
        echo

        # Ensure these config directories are real directories before stow runs.
        # Without this, stow tree-folds the package and links the whole directory
        # straight to the repo, causing machine-local or runtime-generated files to
        # be written physically inside the dotfiles checkout — e.g. zsh/credentials.zsh,
        # gh/hosts.yml (holds auth tokens), alacritty's auto-generated .bak files, or
        # herdr/hunk's session and log files. gh's hosts.yml is covered by a gitignore
        # rule (**/.config/gh/hosts.yml), but zsh's credentials.zsh and herdr/hunk's
        # runtime files are not, so a tree-fold there would leave them sitting in the
        # checkout as regular tracked-looking files.
        local xdg_config="${XDG_CONFIG_HOME:-$HOME/.config}"
        mkdir -p "$xdg_config/zsh" "$xdg_config/gh" "$xdg_config/alacritty" "$xdg_config/herdr" "$xdg_config/hunk"

        stow -R -vvv \
          alacritty \
          any-script-mcp \
          bat \
          gh \
          git \
          herdr \
          hunk \
          nvim \
          starship \
          zsh

        stow -R -vvv -t "$HOME/Library/Application Support/Code/User" vscode
        stow -R -vvv -t "$HOME/Library/Application Support/Cursor/User" cursor
        stow -R -vvv -t "$HOME/.claude" claude

        if [ ! -d $HOME/scripts ]; then
            mkdir $HOME/scripts
        fi
        stow -R -vvv -t "$HOME/scripts" scripts

        mkdir -p "$HOME/.local/bin"
        if ! stow -R -vvv bin; then
            echo "ERROR: 'stow -R bin' failed." >&2
            echo "       Check for conflicting files in ~/.local/bin/ and remove them." >&2
            return 1
        fi

        echo
        echo "🎉 Done!"
        echo
    else
        echo "ERROR: stow is not installed. Run: ./up brew" >&2
        return 1
    fi
}

function install_homebrew()
{
  # Install Homebrew
  if type brew > /dev/null 2>&1; then
    echo 'Homebrew has already been installed.'
  else
    echo 'Install Homebrew'
    # https://brew.sh
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    brew update
  fi

  echo "brew bundle --file=Brewfile.shared"
  if brew bundle --file=Brewfile.shared; then
    echo "Done. Run 'brew bundle --file=Brewfile.private' or 'brew bundle --file=Brewfile.work' to install more dependencies."
  else
    echo "ERROR: 'brew bundle --file=Brewfile.shared' failed." >&2
    return 1
  fi

  install_vscode_extensions
}

# Install VS Code extensions one at a time instead of via `brew bundle`'s
# vscode entries, which install concurrently and race on ~/.vscode/extensions/
# (see https://github.com/chloe463/dotfiles/issues/172).
function install_vscode_extensions()
{
  if ! type code > /dev/null 2>&1; then
    echo "ERROR: 'code' command not found. Run: ./up brew" >&2
    return 1
  fi

  echo 'Installing VS Code extensions...'
  while IFS= read -r extension; do
    [ -z "$extension" ] && continue
    echo "Installing extension: $extension"
    code --install-extension "$extension" --force || {
      echo "WARNING: Failed to install extension: $extension" >&2
    }
  done < "$BASE_DIR/vscode-extensions.txt"
}

function install_global_npm_packages()
{
  if type yarn > /dev/null 2>&1; then
    # yarn global list
    yarn global add \
        create-next-app \
        git-delete-squashed \
        git-split-diffs \
        npkill \
        # prettierd \
        yarn-deduplicate
  else
    echo 'Install yarn first!'
  fi
}

function main()
{
    case $1 in
        "sheldon")
            install_sheldon_plugins
            ;;
        "dot" | "dots" | "stow")
            run_stow
            ;;
        "brew")
            install_homebrew
            ;;
        "npm" | "yarn")
            install_global_npm_packages
            ;;
        *)
            install_homebrew
            install_sheldon_plugins
            run_stow
            install_global_npm_packages
            ;;
    esac
}

main ${1:-""}

