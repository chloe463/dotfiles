#!/bin/zsh

set -eux
set -o pipefail

# emulate GNU readlink's "readlink -f" behavior on Mac
# http://stackoverflow.com/questions/1055671/how-can-i-get-the-behavior-of-gnus-readlink-f-on-a-mac
TARGET_FILE=$0

cd `dirname $TARGET_FILE`
TARGET_FILE=`basename $TARGET_FILE`

# Iterate down a (possible) chain of symlinks
while [ -L "$TARGET_FILE" ]
do
    TARGET_FILE=`readlink $TARGET_FILE`
    cd `dirname $TARGET_FILE`
    TARGET_FILE=`basename $TARGET_FILE`
done

# Compute the canonicalized name by finding the physical path 
# for the directory we're in and appending the target file.
BASE_DIR=`pwd -P`
# PHYS_DIR=`pwd -P`
# RESULT=$PHYS_DIR/$TARGET_FILE
# echo $RESULT
# echo $(dirname $RESULT)
# BASE_DIR=$(dirname $RESULT)

function install_sheldon_plugins()
{
    if type sheldon > /dev/null 2>&1; then
        echo 'Installing sheldon plugins...'
        sheldon lock
    else
        echo 'ERROR: sheldon is not installed. Run: ./up brew' >&2
        return 1
    fi
}

# One-time migration of machine-local files into XDG locations.
# Idempotent: each move runs only when the legacy path exists and the XDG
# destination does not, so re-running is a no-op.
function migrate_to_xdg()
{
    local xdg_config="${XDG_CONFIG_HOME:-$HOME/.config}"
    local xdg_data="${XDG_DATA_HOME:-$HOME/.local/share}"

    # Ensure ~/.config/zsh is a real directory before stow runs. Without this,
    # stow tree-folds the new package and links ~/.config/zsh straight to the
    # repo, causing machine-local files (e.g. credentials.zsh) to be written
    # inside the dotfiles checkout.
    mkdir -p "$xdg_config/zsh"

    # bun: ~/.bun -> $XDG_DATA_HOME/bun
    if [ -d "$HOME/.bun" ] && [ ! -e "$xdg_data/bun" ]; then
        echo "Migrating ~/.bun -> $xdg_data/bun"
        mkdir -p "$xdg_data"
        mv "$HOME/.bun" "$xdg_data/bun"
    fi

    # credentials: ~/.credentials.zsh -> $ZDOTDIR/credentials.zsh
    if [ -f "$HOME/.credentials.zsh" ] && [ ! -e "$xdg_config/zsh/credentials.zsh" ]; then
        echo "Migrating ~/.credentials.zsh -> $xdg_config/zsh/credentials.zsh"
        mv "$HOME/.credentials.zsh" "$xdg_config/zsh/credentials.zsh"
    fi

    # fzf: ~/.fzf.zsh -> $XDG_CONFIG_HOME/fzf/fzf.zsh
    if [ -f "$HOME/.fzf.zsh" ] && [ ! -e "$xdg_config/fzf/fzf.zsh" ]; then
        echo "Migrating ~/.fzf.zsh -> $xdg_config/fzf/fzf.zsh"
        mkdir -p "$xdg_config/fzf"
        mv "$HOME/.fzf.zsh" "$xdg_config/fzf/fzf.zsh"
    fi
}

function run_stow()
{
    if type stow > /dev/null 2>&1; then
        echo "Making symlinks of dot files..."
        echo

        stow -R -vvv \
          alacritty \
          any-script-mcp \
          bat \
          gh \
          git \
          nvim \
          starship \
          tmux \
          vim \
          zsh

        stow -R -vvv -t "$HOME/Library/Application Support/Code/User" vscode
        stow -R -vvv -t "$HOME/Library/Application Support/Cursor/User" cursor
        stow -R -vvv -t "$HOME/.claude" claude

        if [ ! -d $HOME/scripts ]; then
            mkdir $HOME/scripts
        fi
        stow -R -vvv -t "$HOME/scripts" scripts

        echo
        echo "🎉 Done!"
        echo
    else
        echo "Install stow first!"
    fi
}

function install_homebrew()
{
  # Install Homebrew
  if type brew > /dev/null 2>&1; then
    echo 'Homebrew has already been installed.'
  else
    echo 'Install Homebrew'
    # https://brew.sh
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
    brew update
  fi

  echo "brew bundle --file=Brewfile.shared"
  if brew bundle --file=Brewfile.shared; then
    echo "Done. Run 'brew bundle --file=Brewfile.private' or 'brew bundle --file=Brewfile.work' to install more dependencies."
  else
    echo "ERROR: 'brew bundle --file=Brewfile.shared' failed." >&2
    return 1
  fi
}

function install_global_npm_packages()
{
  if type yarn > /dev/null 2>&1; then
    # yarn global list
    yarn global add \
        create-next-app \
        git-delete-squashed \
        git-split-diffs \
        npkill \
        # prettierd \
        yarn-deduplicate
  else
    echo 'Install yarn first!'
  fi
}

function install_tmux_plugins()
{
  if type tmux > /dev/null 2>&1 ; then
    if [ -d ~/.tmux/plugins/tpm ]; then
      echo 'tpm is already installed!'
    else
      echo 'install tpm'
      # https://github.com/tmux-plugins/tpm
      git clone https://github.com/tmux-plugins/tpm.git ~/.tmux/plugins/tpm

      echo 'Create tmux session and run the following command in the session.'
      echo 'prefix + I (ctrl + t → I)'
    fi
  else
    echo 'Install tmux first!'
  fi
}

function main()
{
    case $1 in
        "sheldon")
            install_sheldon_plugins
            ;;
        "dot" | "dots" | "stow")
            migrate_to_xdg
            run_stow
            ;;
        "brew")
            install_homebrew
            ;;
        "npm" | "yarn")
            install_global_npm_packages
            ;;
        "tmux")
            install_tmux_plugins
            ;;
        *)
            install_homebrew
            install_sheldon_plugins
            migrate_to_xdg
            run_stow
            install_global_npm_packages
            install_tmux_plugins
            ;;
    esac
}

main ${1:-""}

